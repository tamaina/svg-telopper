doctype html
html(lang="ja")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible" content="ie=edge")
    title SVG Telopper!: Setting

    script
      block version
        | var version = "#{pkg.version}#{salt}";
      block node_env
        | var nodeEnv = "#{env}";
      block locale
        | var langs = !{langs};
    script(src=`./assets/setting-boot.js?${pkg.version}${salt}`)
    link(href='https://fonts.googleapis.com/css?family=Roboto:100,300,400,500,700,900|Material+Icons' rel="stylesheet")
    script(src=`./assets/setting.js?${pkg.version}${salt}` defer)

  body
    noscript
      | てぇ～へんだ てーへんだーー！ JavaScriptが有効になってないってぇ！？
      a(href="https://a9z.dev/products/svg-telopper/v2.0/") README
    div#app
